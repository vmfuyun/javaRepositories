  
 
 1)包名如何定义  
         1) 包名也是标识符(全部小写)

         2)  企业开发中,包名才有公司域名倒写  
                   如果域名是数字开头的 尽量再填加别的字母


 2)类的名称
        
    类的简单名称 
    类的全部位置名称 (  就是哪个包的 或者哪个子包,子子包的类)




package最佳实践：
1、包名如何定义：
        1）包名必须遵循标识符规范（不能是关键字、保留字，不能以数字开头）/全部小写
        2）企业开发中，包名采用公司域名倒写
        小码哥域名：520it.com-->com.520it-->此时520it不符合标识符规范（不能以数字开头），我们可以使用下划线：com._520it
         在Android中，如果package中使用了_，则不能部署到模拟器，此时，我们也可以使用一个字母来代替_。
        比如：package com.m520it;        package com.a520it
        格式：package 域名倒写.模块名.组件名；
        例如： package com._520it.pss.util;
2、类的名称：
        类的简单名称：定义类的名称   PackageDemo   
        类的全限名称：包名.类名   com._520it.PackageDemo
3、在开发中，都是先有package，而后在package中再定义类