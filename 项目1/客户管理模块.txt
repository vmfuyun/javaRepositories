按着 该页面 有什么 就按着这个 在对应的conterllor 添加 对应的查询 数据

1, 客户池 是一个没有跟进..一类的    有一个移交和吸纳
2,跟进 进行 客户历史 保存    
  移交历史  
	开始
		1,创建顾客表 Customer,把外键列 替换成对应的维护客户的对象  也见名知意
		2,把状态 或者把用来分类的 关键 	处理成常量   ,直观, 受检查  
		3,  模态框的 使用的是 把数据绑定在 按钮上  ..所以不需要sql查询单个
		4,修改mapper.xml     修改sql   
		//开始
		5,先显示 列表的 然后通过starts来拼接条件来 显示   (高级查询)方式
		6,登陆完成后 在list来判断  超管和 经理(通过shiro来获取) 	 因为是当登陆之后才能能去判断查询 
			实现 只有超管和经理(角色) 才能看到所有的客户,其余的 拼接sql 通过高级查询条件
		7,高级查询  功能的实现  市场专员的确定 
				通过角色的sn  来确定  确定一个方法  通配 查询 怎么做?  
						通过传的参数 是一个可变参数来 实现 统配查询 
		8,添加,编辑   添加需要查询出 需要的下来框 因为在一个页面 所以要通过list 来实现
			编辑 需要回显  需要封装的 json >> map                没有设置的字段需要去掉
		9,修改状态   
		10,跟进历史 需要的功能
			注意时间格式化(前端 freeMarder   ftl文件 使用)--->  <td>${entity.traceTime?string('yyyy-MM-dd')}</td>  
		11,移交:   解决获取当前登陆人员的信息-->通过shiro标签	 $("input[name='newSeller.id']").val(<@shiro.principal property = "id"/>);
			先看移交历史---修改表名  生成domain  修改关联对象 
			mapper 看需要留什么-- insert 和 selectForlist
	

	报表:  按着年月日 分组  通过 DATE_FORMAT (时间,'%Y-%M-%d")-----
	没有对应的实体类的查询 需要用 map   
		1 map集合 对应 一行 数据      map的key是列名 value对应的就是   那个行的值   ,,,,一个map      列有几个 put几个    ,,,,多行数据 就用 list 
	注意一个 map记录一行数据 列名是 key   所以要尽量 指定这个列名是什么(固定一个) 更好的来 获取对应的值   
						如果 页面需要的位置是一个  不可遍历的    需要想到用 list集合变成 json   它会自动显示出来
	      sql语句 Group by 后面 (通过什么来分组的)  select 后面必须有   (多个表查询 会用别名  所以 一般情况 会把 查询出来的 结果再 起别名)
	SpringMVC 封装的 时间 需要用 @dataformak---来标志接受  
	分组类型  直接用 对应的  拼接条件来实现    注意传的时候  mapper要用 ${} 来接受 因为有group  by 

	柱状图 需要 data属性值 修改     
	    ECharts     

	模态框 显示 柱状图一类    地址 绑定在按钮上                         
	                                       弹出 模态框的时候  使用的是  一个remote : url ====   但是每次进去之前 需要 清除缓存 ..   直接去百度
			                                         $("模态框").modal({remote: url?xxx})  		$("模态框").remove("bs.modal")
		也就是说 模态框  是先去 通过 conterllor 去访问 然后 得到数据 回显 到 报表   所以 需要 访问之前 把 对应的时候 传过去
		 序列表表格内容为字符串   serialize() 通过这个可以获取到 表单的信息  给 conterllor 数据 来动态的生成当前页所查的信息
	java 代码快速 替换 map中的 key值 用的是  map.put("value",map.remove("number"));
  

	js 中的this 如果是事件中的话 指向 事件源  
	
	mvc 返回 json   是异步请求   肯定是没有跳转的  ...需要跳转 肯定不能 返回 json