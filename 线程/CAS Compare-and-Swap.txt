CAS原理:
	包含三个值:
	内存值:v,
	预估值:A,
	更新值:B
	当且仅当  内存值V==A, 才会运行 V=B;  否则 不会执行任何操作.
	
	和乐观锁很相似  条件是  内存值==预估值,  
	 相当于变量值  有一个顺序 也就是说当两个线程都获取到内存值,当一个线程改变的时候, 内存值就变了, 
	 第二个线程 进去的时候,一修改 然后比较内存值 和 这个线程获取到的原先的 预估值 不一样,所以就不能修改
