调用父类的构造方法我们使用super()即可。


 super为父类地址  
  
 this 为先类地址


什么是继承关系：
基于某个父类对对象的定义加以扩展，而产生新的子类，子类可以继承父类原来的某些定义，也可以增加 原来父类没有的定义，或者复写父类中的某些特性。

从面向对象的角度来说：继承是一种从一般到特殊的关系，是一种“is a ”的关系，即子类是对父类的扩展，是一种特殊的父类。

在Java语言中，存在多个类的时候，我们使用“extends”关键字来表示子类和父类之间的关系。
语法格式：在定义子类的时候来表明自己需要扩展于哪一个父类。

public class  子类类名  extends   父类类名
{
          编写自己特有的状态和行为
}

在Java中。类和类之间只允许单继承，不允许多继承，也就是说一个类A，只能有一个之间父类，不能出现类A同时继承于类B和类C。
但是Java中允许多重继承。

在Java中，除了Object类之外，每一个类都有一个直接父类。Object类是Java语言的根类，它要么就是一个类的直接父类，要么是一个类的间接父类。

继承关系的作用：
1、解决代码重复的问题
2、真正的作用，表示一个体系
3、继承人可以复写父类的方法，也可以不写。 写的话，拥有自己的特性。不写的话，拥有父类的方法特性。