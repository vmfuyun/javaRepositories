内网穿透   natApp
	工作原理:
	加密   发送的是 私钥加密   --验证是 使用的是公钥
	一开始 使用的是html文件的形式是 提交表单; active 是阿里的地址--此时的参数也就是表单的参数
	然后调转到阿里
	阿里重定向 通过get 形式 把参数传到 我们的应用上 我们通过解密 获取参数
		(同步通知的弊端)重定向会有一个问题就是: 当我们把浏览器关了  就收不到对应的参数了

	异步方式:  支付宝   定时的向服务器,发送请求并带上参数,,(POST请求),   服务器也必须有一个接口来接受: 并处理参数,验证-修改订单状态,
							                                       并必须要响应对应的字符 success	    每25小时会发起8次请求
	
	同步通知 : 我们只 写一些 正常的页面   
	我们只在异步方式的 位置 写业务             一般支付是单独一个微服务在公司中
	
	创建一个alipay.yml,创建alipayproerties,Config 并把client创建出来 
	把异步同步的设置一下,参数也写下,,,      写出对应值 和 写出
	
	然后是同步url    异步url
接口调用--->>>	要实现幂等性   ---使用一个支付 表  的  order来实现注意
	 
	