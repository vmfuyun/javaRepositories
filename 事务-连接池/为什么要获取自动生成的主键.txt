使用快速注册的案例来分析:
1. 早期:注册账号,需要填入很多数据.只需要执行插入操作.但是用户体验不好.
2. 快速注册:只需要账号和密码即可,该操作是插入操作,如果要完善资料,在执行更新操作.

怎么获取 主键信息呢  

	需要 添加一个标记 (是否要获取自动生成的id)     意识要标记的话   一般是传常量
Statement 模式    create ---executeUpdate  sql 标记(Statement.return_generaed)    执行完毕后    resultSet .           通过 prepareStatement      getGener..keys

PreparedStatement   ---                                                           ResultSet      getmetaData


Statement:
int executeUpdate(String sql,int autoGeneratedKeys):执行SQL:
         参数:autoGeneratedKeys,是否需要返回自动生成的主键.常量值:Statement.RETURN_GENERATED_KEYS 
ResultSet getGeneratedKeys():获取自动生成的主键
PreparedStatement:
PreparedStatement prepareStatement(String sql, int autoGeneratedKeys)  :创建PreparedStatement对象,病指定是否需要返回生成的主键. 参数的常量值:Statement.RETURN_GENERATED_KEYS
