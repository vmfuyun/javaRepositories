分页的原理   目的
全部显示出来容易内存溢出   也不利于查看

limit 需要的是 (页数-1)*每页数据数, 每页数据数
count(*)	函数查看符合条件的总页数

注意 条件 

两个客户端给的两个条件
int 	current page   //当前页 
int  	count page   //当前页的数据条数

两个查询出来的的数据
List   date                //查询出来的数据		//sql语句
int    count  	//数据总条数

三个查询出来的数据  
int     prevpage  	//上一页  
int     nextpage        //下一页
int     totalpage       //尾页		//通过分类来 记忆  一般都能实现  ----

去查询  带着查询对象        必须是
完毕   带着结果对象   结果对象必须要和前端的所需要的参数对应上


1,用queryObject类来封装  sql语句需要的两个参数----------------建立的单独query包
	ONGL表达式  ---取属性值#{属性名}, map #{key},-
2,把dao层测试完毕      -------两个语句
3,把本次查询,前端需要的参数封装到一个对象里(类), ---也叫结果集---------------前端显示的参数过多的话.就可以封装在一个类
    分析 前端参数之间的关系  和 所必须的参数  (弄出最少的参数表)进行封装
4,service- 当判断 数据记录数为0 直接调用 空的构造器(集合为空,记录数为0)---在模板类加一个空集合,数据记录数为0
	要测试service层方法----都放在test包里
5,view   通过service来获取数据    展现在jsp  当输入的时候 是一个表单
