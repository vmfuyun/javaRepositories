原因:  1,网络延迟
          2,成功刷新
          3,回退 
根本原因: 没有完成   请求页面-->提交数据   保证证两个是完整性    通过session来保证 
 当登陆之前 用一个 servlet来设置   中间的页面接收下 用input标签的 type (hi..)       然后 通过第二个 servlet 获取session 的数据  看看是不是一致  
	一致 转账   销毁 session  
	不一致  不做处理 
	
	token   令牌机制

	演示 预防表单重复提交
  	过程  一先创建一个uuid  
  		二把数据共享给session
  		三把数据也分享给req 
  		四 跳转到另一个 页面后 进行比较 
		五 跳转到另一个 页面后  利用 一个隐藏的input 控件来接受 参数  以便 后面的servlet接受   后面的比较  
				  -----令牌机制  两个共享 中间的要接受  利用EL表达式,隐藏的一个控件   来 接受 
		六 第二个 servlet 通过 名字来找到 上一个servlet 设置的参数 
		七 注意 要把 要正常进行的   要安排好