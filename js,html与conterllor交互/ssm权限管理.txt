1,  当看页面没有信息的话   查看一下 页面的源码 看看对不对  有没有 ,,,,看 sql语句有没有发      剩下的 就是页面有问题  看看是不是有


在jsp 中 判空   使用  <c:if test="${empty entity.id}">     注意 是el表达式里面的  


js 的事件绑定  处理   注意 移动的话  需要把 先把获取的东西打印一下 然后去看看  是不是获取对了   移动的话 一般 就用appendto---

知识点  细节 
	1,启动的时候 会加载文件  例如:applictionContext.xml文件 .mvc.xml文件 一类   :::有的时候 没有加载到文件也会报错:: 看加载的文件对不对,,是不是少了
	2,共同的jsp可以放在 一个文件里  通过<%include   路径-----%> 来引入    解决页面东西太多
	3,分析 要处理的东西 例如 一个 检查是否有用户名  等等 功能    ,应该放在哪   一般是分析 这个功能是什么时候要用 在什么之前,什么之后	
	4,如果有类型 是要赋默认值的话,那就用  基本类型------
	5,如果有的字段是两个 大写的连着(开头) 例如:EDept   这样的 属性是 EDept 
	6,多表联合 left jion 查询 需要把重复的起别名               来更好的封装  <association  来封装   注意 需要 属性 和 封装成的类型  然后里面进行封装
	7,一般多表联合查询的话,   注意关联的字段  就直接去掉吧 ----然后用多表查询出来 回显用 get方法
	8,注意sql 语句中  or 是最后运行的,      问题 是让它提前运行  加括号
	9, 需要判断的东西 例如:拼接sql的条件  就尽量 放到java代码里 判断  尽量让 dao层 判断简单:   例如字符串的空值判断 stringutil 如果是空就返回一个null 用三元
	10,el 表达式 需要 判断 空不空  ;;; 用 <c:if   text="${empty 值}">  这个要注意
	11,复选框  发送消息的时候  空的话 不发 或者 非空 发 on
	12,下拉框的回显 可以用js  $("select[name=${回显值}]").prop("selected",true)  ---设置属性来实现回显;;;;;   注意属性的增加方法  Attr  或者 prop
	13,多选框的 回显 去重    ----思路:回显的话 ,,可以把数据共享出来 然后用 C:foreach 来显示.....  
			    去重 :  $()来获取 标签的 jquery对象 然后用$.foreach 来遍历 ..一般是第二个参数是doc对象 就可以获取到值,,然后放到一个数组中 用 数组.push()加入
				然后通过 通过 $.inArray 判断有没有这个-= 值.. 如果有的话 直接用doc对象.remove 删除 
	14,移位置  通过 全部移动:::用选择器   然后  appendTo--- 
			挑选移动 通过选择器 筛选器来筛选出 选中的---
	15,提交 --之前 (全选--做一些 需要做的)   1,获取到表单元素;;注意是表单元素<提示:$()  ..2 使用submit绑定事件---然后如果是多选框的是 添加属性
	16,抓住一点,一点点展开----
	17,需求: 让有些元素(下拉框一类的) 被 一个点击 单选框来判断 要不要显示()
				通过this的checked  是不是true来判断    一般删除的话 需要判断有没有事件  要回显  如果要回显的话就用detach
	18,登陆界面  
		注意,获取前端传过来的数据,是需要前端的name 的名字 和 接受的变量是要一致的;
		共享的数据: 有登陆的人员信息 和 它对应的权限List<String>,权限 
					多表(三个表以上)查出来  需要 去重(去重方式1,sql语句上 使用distinct  2,使用set集合 去重 )
	
	19,注销 操作     销毁session对象 用invalidate方法 (销毁)
	20,拦截器 两个方式  实现或者继承方式  intercept   才能变成拦截器  (注意拦截的放行 要是mvc的路径 <关键> )
			1,检查登陆信息    session.attr 是不是null  不是的话 重定向到登陆界面	
			2, 权限检查 拦截器    安全检查   看最后的流程  一般看需求
							拦截的信息 都存放在 obj里面   也就是handler里面
	21,优化  要求  一般业务层 是 不能用mvc 的对象 
		 业务层共享到界面的数据session...一类的 需要用 工具类
		工具  把名称XXX_IN_SESSION 设置成常量
			把数据 构建成方法---get  set  更好的操作	


总结:
	项目开始的流程 ;
	知识点的熟悉程度 :----尽量做到  要分析好 --然后去下手(分步骤 分析)-
	做一个 看效果 看一个 试一个
	
	