	JSON（JavaScript Object Notation, JS 对象简谱）是一种轻量级的数据交换格式。
	
格式
1）映射用冒号（“:”）表示。“名称” : 值，标准格式名称用双引号括起来；
2）并列的数据之间用逗号（“,”）分隔。“名称1” : 值1, ”名称2“ : 值2；
3） 映射的集合（对象）用大括号（“{}”）表示。{“名称1” : 值1, “名称2” : 值2}
4） 并列数据的集合（数组）用方括号(“[]”)表示。
         [
           {“名称1” : 值, “名称2” : 值2},
           {“名称1” : 值, ”名称2“ : 值2}
         ]
       5）元素值可具有的类型：string, number, object, array, true, false, null



json库
	
开发中一般都会使用第三方的一些 JSON 操作的库来来完成 Java 对象与 JSON 字符串之间的转换。
在 Java 中，转换 JSON 的库有很多，这里单讲两种常用：
Jackson：在 SpringMVC 中内置的一个转换 JSON 的插件，速度也挺快，稳定性比较好。
Fastjson：阿里出品，号称是 Java 领域中转换 JSON 最快的一个插件，中文文档比较齐全。用起来比较友好的    Java 对象 <---> JSON 字符串

当元素失去焦点时触发 blur 事件。---就是光标离开了 这个位置


标签 问题
	                这个注解的意思是 下面的返回时 json格式语句
注意：处理方法贴有 @ResponseBody。  (响应的宝贝)
	类下的方法全是json格式的语句           
若类贴的注解是 @RestController，(返回json格式的 并且 是处理器(记前三个的单词))则响应 JSON 数据的处理方法不需贴 @ResponseBody。

以上 贴了标签的 返回可以是一个对象 集合...--

js  是通过json 进行 parse 解析的